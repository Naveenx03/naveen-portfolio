# Backend Projects (Java + JDBC + MySQL)

This folder contains projects where I use Java with MySQL databases using JDBC.

These projects include CRUD operations, DAO layers, database connectivity, and backend logic.
They demonstrate my skills in writing structured backend code without frameworks.

